import React from 'react';
import StaffImage from '../../../../resources/staff.jpg';

const main = {

};

const Content = (props) => {
	return (
		<div>
			{props.children}
		</div>
	);
};

export default Content;
